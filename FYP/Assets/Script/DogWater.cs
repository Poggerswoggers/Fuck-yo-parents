using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DogWater : MonoBehaviour
{
    public bool fatass = true;
    // Start is called before the first frame update
    void Start()
    {
        fatass = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
